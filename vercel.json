{
  "version": 2,
  "buildCommand": "npm run build",
  "functions": {
    "api/webhook.ts": {
      "maxDuration": 30
    }
  },
  "routes": [
    {
      "src": "/api/webhook",
      "dest": "/api/webhook.ts"
    }
  ]
}
